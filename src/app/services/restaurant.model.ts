export interface Restaurant {
    restaurantID: number;
    name: string;
    location: string;
    phoneNumber: string;
    isActive: string; // Will display as "Online" or "Offline" in the frontend
  }
  